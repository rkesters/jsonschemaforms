{
	"type": "object",
	"definitions": {
		"interface": {
			"type": "object",

			"properties": {
				"IPV": {
					"title": "IP Version",
					"enum": ["IPv4", "IPv6"]
				},
				"address": {
					"type": "string",
					"format": "ipv4"
				}
			}
		}
	},
	"properties": {
		"interfaces": {
			"type": "array",
			"items": { "$ref": "#/definitions/interface" }
		},
		"commandAddress": {
			"title": "Command Address",
			"type": "string",
			"$patch": ""
		}
	},
	"required": ["interfaces", "commandAddress"]
}
